var assert = require('assert');
var swappers = require('../util/swapKeysValues.js');
var swap = swappers.swap;

var obj1 = {a: 'one', b: 'two', c: 'three'};
var obj2 = {one: 'a', two: 'b', c: 'c'};

describe('swap', function() {
  it("when each key has a value, should return a new object with keys swapped with values", function() {
    
	});
});
